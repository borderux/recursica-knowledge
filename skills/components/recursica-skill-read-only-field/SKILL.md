---
name: recursica-skill-read-only-field
description: Trigger this when the developer asks to design, write, or refactor a UI layout containing the read-only-field component.
license: MIT
metadata:
  author: hi@borderux.com
  version: 0.1.0
---

# ReadOnlyField Skill

**A read-only field is used to display data in a form-like structure without allowing direct user input. It maintains visual consistency with other form fields but presents information that is not currently editable.**

## Anatomy & Sub-components

This skill covers the following component specs defined in the UI Kit:

- `read-only-field`

---

## When to Use

- **Displaying saved data**: Use it in "view mode" on pages like user profiles or settings to show existing information like a name, email, or ID number.
  - **Confirmation screens**: To present a summary of the data a user has just entered for their review before final submission.
  - **System generated values**: To show values that are generated by the system and cannot be changed by the user, such as an "Account ID" or "Date Created."
  - **Accessibility & Best Practices**: Clearly style read-only fields so they aren't confused with disabled or editable fields.

---

## When Not to Use

- **User input**: If the user needs to enter or modify data, use an active component like a text input.
  - **Instead of disabled fields**: A disabled field implies that a field could become active if other conditions are met. A read-only field simply displays data. Use a disabled field if you want to retain the input styling but block interaction.
  - **For non-form content**: If the information is not part of a form and doesn't require a label / value pairing, use standard text elements instead.
  - **Anti-patterns**: Don't use a read-only field if the user can never edit the data in any context; just use plain text.

---

## Best Practices

- Follow platform accessibility guidelines.
- Ensure consistent padding and alignments.

---

## Referential Libraries & Documentation

- Carbon Design System: [Carbon ReadOnlyField Documentation](https://carbondesignsystem.com)
- Material UI: [MUI ReadOnlyField Documentation](https://mui.com)
