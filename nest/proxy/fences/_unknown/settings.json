{
  "$comment": "The deny-all fallback, and the template for a real fence. Copy this directory to fences/<name>/, then put that community's MCP servers in a .claude.json beside it. _unknown itself gets none, on purpose.",
  "disableClaudeAiConnectors": true,
  "permissions": {
    "deny": [
      "Read(~/.claude.json)",
      "Read(~/.claude/**)",
      "Read(~/.buzz/proxy/fences/**)",
      "Read(~/.buzz/.secrets/**)"
    ]
  }
}
